<%= form_for([@question, @question.responses.build]) do |f| %>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text, :size => '40x5'  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
